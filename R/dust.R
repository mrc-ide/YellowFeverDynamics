## Generated by dust (version 0.9.8) - do not edit
BasicModelOD <- R6::R6Class(
  "dust",
  cloneable = FALSE,

  private = list(
    pars_ = NULL,
    pars_multi_ = NULL,
    index_ = NULL,
    info_ = NULL,
    n_threads_ = NULL,
    n_particles_ = NULL,
    n_particles_each_ = NULL,
    shape_ = NULL,
    ptr_ = NULL,
    device_id_ = NULL,
    param_ = list(dP1_all = list(has_default = FALSE, default_value = NULL, rank = 2, min = -Inf, max = Inf, integer = FALSE),
     dP2_all = list(has_default = FALSE, default_value = NULL, rank = 2, min = -Inf, max = Inf, integer = FALSE),
     dt = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     Exp0 = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     FOI_spillover = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     Inf0 = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     N_age = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     n_years = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     R0 = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     Rec0 = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     Sus0 = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     Vac0 = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     vacc_rate_annual = list(has_default = FALSE, default_value = NULL, rank = 2, min = -Inf, max = Inf, integer = FALSE),
     vaccine_efficacy = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     year0 = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE))
  ),

  public = list(
    initialize = function(pars, step, n_particles, n_threads = 1L,
                          seed = NULL, pars_multi = FALSE, device_id = NULL) {
      res <- dust_BasicModelOD_alloc(pars, pars_multi, step, n_particles,
                        n_threads, seed, device_id)
      private$pars_ <- pars
      private$pars_multi_ <- pars_multi
      private$n_threads_ <- n_threads
      private$ptr_ <- res[[1L]]
      private$info_ <- res[[2L]]
      private$shape_ <- res[[3L]]
      private$device_id_ <- res[[4L]]
      private$n_particles_ <- prod(private$shape_)
      if (pars_multi) {
        private$n_particles_each_ <- private$n_particles_ / length(pars)
      } else {
        private$n_particles_each_ <- private$n_particles_
      }
    },

    name = function() {
      "BasicModelOD"
    },

    param = function() {
      private$param_
    },

    run = function(step_end, device = FALSE) {
      m <- dust_BasicModelOD_run(private$ptr_, step_end, device)
      rownames(m) <- names(private$index_)
      m
    },

    simulate = function(step_end, device = FALSE) {
      m <- dust_BasicModelOD_simulate(private$ptr_, step_end, device)
      rownames(m) <- names(private$index_)
      m
    },

    set_index = function(index) {
      dust_BasicModelOD_set_index(private$ptr_, index)
      private$index_ <- index
      invisible()
    },

    index = function() {
      private$index_
    },

    n_threads = function() {
      private$n_threads_
    },

    n_state = function() {
      dust_BasicModelOD_n_state(private$ptr_)
    },

    n_particles = function() {
      private$n_particles_
    },

    n_particles_each = function() {
      private$n_particles_each_
    },

    shape = function() {
      private$shape_
    },

    set_state = function(state, step = NULL) {
      dust_BasicModelOD_set_state(private$ptr_, state, step)
      invisible()
    },

    reset = function(pars, step) {
      private$info_ <- dust_BasicModelOD_reset(private$ptr_, pars, step)
      private$pars_ <- pars
      invisible()
    },

    set_pars = function(pars) {
      private$info_ <- dust_BasicModelOD_set_pars(private$ptr_, pars)
      private$pars_ <- pars
    },

    state = function(index = NULL) {
      m <- dust_BasicModelOD_state(private$ptr_, index)
      rownames(m) <- names(index)
      m
    },

    step = function() {
      dust_BasicModelOD_step(private$ptr_)
    },

    reorder = function(index) {
      storage.mode(index) <- "integer"
      dust_BasicModelOD_reorder(private$ptr_, index)
      invisible()
    },

    resample = function(weights) {
      invisible(dust_BasicModelOD_resample(private$ptr_, weights))
    },

    info = function() {
      private$info_
    },

    pars = function() {
      private$pars_
    },

    rng_state = function(first_only = FALSE, last_only = FALSE) {
      dust_BasicModelOD_rng_state(private$ptr_, first_only, last_only)
    },

    set_rng_state = function(rng_state) {
      dust_BasicModelOD_set_rng_state(private$ptr_, rng_state)
      invisible()
    },

    has_openmp = function() {
      dust_BasicModelOD_capabilities()[["openmp"]]
    },

    has_cuda = function() {
      dust_BasicModelOD_capabilities()[["cuda"]]
    },

    n_pars = function() {
      if (private$pars_multi_) length(private$pars_) else 0L
    },

    set_n_threads = function(n_threads) {
      prev <- private$n_threads_
      dust_BasicModelOD_set_n_threads(private$ptr_, n_threads)
      private$n_threads_ <- n_threads
      invisible(prev)
    },

    has_compare = function() {
      dust_BasicModelOD_capabilities()[["compare"]]
    },

    set_data = function(data) {
      dust_BasicModelOD_set_data(private$ptr_, data)
    },

    compare_data = function(device = FALSE) {
      dust_BasicModelOD_compare_data(private$ptr_, device)
    },

    filter = function(save_trajectories = FALSE, step_snapshot = NULL,
                      device = FALSE) {
      dust_BasicModelOD_filter(private$ptr_, save_trajectories,
                           step_snapshot, device)
    },

    device_info = function() {
      ret <- dust_BasicModelOD_device_info()
      if (ret$has_cuda && exists("private", inherits = FALSE)) {
        ret$device_id <- private$device_id_
      }
      ret
    }
  ))
class(BasicModelOD) <- c("dust_generator", class(BasicModelOD))
FullModelOD <- R6::R6Class(
  "dust",
  cloneable = FALSE,

  private = list(
    pars_ = NULL,
    pars_multi_ = NULL,
    index_ = NULL,
    info_ = NULL,
    n_threads_ = NULL,
    n_particles_ = NULL,
    n_particles_each_ = NULL,
    shape_ = NULL,
    ptr_ = NULL,
    device_id_ = NULL,
    param_ = list(Cas0 = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     dP1_all = list(has_default = FALSE, default_value = NULL, rank = 2, min = -Inf, max = Inf, integer = FALSE),
     dP2_all = list(has_default = FALSE, default_value = NULL, rank = 2, min = -Inf, max = Inf, integer = FALSE),
     dt = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     Exp0 = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     FOI_spillover = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     Inf0 = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     N_age = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     n_years = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     R0 = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     Rec0 = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     Sus0 = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     Vac0 = list(has_default = FALSE, default_value = NULL, rank = 1, min = -Inf, max = Inf, integer = FALSE),
     vacc_rate_annual = list(has_default = FALSE, default_value = NULL, rank = 2, min = -Inf, max = Inf, integer = FALSE),
     vaccine_efficacy = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE),
     year0 = list(has_default = FALSE, default_value = NULL, rank = 0, min = -Inf, max = Inf, integer = FALSE))
  ),

  public = list(
    initialize = function(pars, step, n_particles, n_threads = 1L,
                          seed = NULL, pars_multi = FALSE, device_id = NULL) {
      res <- dust_FullModelOD_alloc(pars, pars_multi, step, n_particles,
                        n_threads, seed, device_id)
      private$pars_ <- pars
      private$pars_multi_ <- pars_multi
      private$n_threads_ <- n_threads
      private$ptr_ <- res[[1L]]
      private$info_ <- res[[2L]]
      private$shape_ <- res[[3L]]
      private$device_id_ <- res[[4L]]
      private$n_particles_ <- prod(private$shape_)
      if (pars_multi) {
        private$n_particles_each_ <- private$n_particles_ / length(pars)
      } else {
        private$n_particles_each_ <- private$n_particles_
      }
    },

    name = function() {
      "FullModelOD"
    },

    param = function() {
      private$param_
    },

    run = function(step_end, device = FALSE) {
      m <- dust_FullModelOD_run(private$ptr_, step_end, device)
      rownames(m) <- names(private$index_)
      m
    },

    simulate = function(step_end, device = FALSE) {
      m <- dust_FullModelOD_simulate(private$ptr_, step_end, device)
      rownames(m) <- names(private$index_)
      m
    },

    set_index = function(index) {
      dust_FullModelOD_set_index(private$ptr_, index)
      private$index_ <- index
      invisible()
    },

    index = function() {
      private$index_
    },

    n_threads = function() {
      private$n_threads_
    },

    n_state = function() {
      dust_FullModelOD_n_state(private$ptr_)
    },

    n_particles = function() {
      private$n_particles_
    },

    n_particles_each = function() {
      private$n_particles_each_
    },

    shape = function() {
      private$shape_
    },

    set_state = function(state, step = NULL) {
      dust_FullModelOD_set_state(private$ptr_, state, step)
      invisible()
    },

    reset = function(pars, step) {
      private$info_ <- dust_FullModelOD_reset(private$ptr_, pars, step)
      private$pars_ <- pars
      invisible()
    },

    set_pars = function(pars) {
      private$info_ <- dust_FullModelOD_set_pars(private$ptr_, pars)
      private$pars_ <- pars
    },

    state = function(index = NULL) {
      m <- dust_FullModelOD_state(private$ptr_, index)
      rownames(m) <- names(index)
      m
    },

    step = function() {
      dust_FullModelOD_step(private$ptr_)
    },

    reorder = function(index) {
      storage.mode(index) <- "integer"
      dust_FullModelOD_reorder(private$ptr_, index)
      invisible()
    },

    resample = function(weights) {
      invisible(dust_FullModelOD_resample(private$ptr_, weights))
    },

    info = function() {
      private$info_
    },

    pars = function() {
      private$pars_
    },

    rng_state = function(first_only = FALSE, last_only = FALSE) {
      dust_FullModelOD_rng_state(private$ptr_, first_only, last_only)
    },

    set_rng_state = function(rng_state) {
      dust_FullModelOD_set_rng_state(private$ptr_, rng_state)
      invisible()
    },

    has_openmp = function() {
      dust_FullModelOD_capabilities()[["openmp"]]
    },

    has_cuda = function() {
      dust_FullModelOD_capabilities()[["cuda"]]
    },

    n_pars = function() {
      if (private$pars_multi_) length(private$pars_) else 0L
    },

    set_n_threads = function(n_threads) {
      prev <- private$n_threads_
      dust_FullModelOD_set_n_threads(private$ptr_, n_threads)
      private$n_threads_ <- n_threads
      invisible(prev)
    },

    has_compare = function() {
      dust_FullModelOD_capabilities()[["compare"]]
    },

    set_data = function(data) {
      dust_FullModelOD_set_data(private$ptr_, data)
    },

    compare_data = function(device = FALSE) {
      dust_FullModelOD_compare_data(private$ptr_, device)
    },

    filter = function(save_trajectories = FALSE, step_snapshot = NULL,
                      device = FALSE) {
      dust_FullModelOD_filter(private$ptr_, save_trajectories,
                           step_snapshot, device)
    },

    device_info = function() {
      ret <- dust_FullModelOD_device_info()
      if (ret$has_cuda && exists("private", inherits = FALSE)) {
        ret$device_id <- private$device_id_
      }
      ret
    }
  ))
class(FullModelOD) <- c("dust_generator", class(FullModelOD))
## Generated by odin.dust (version 0.2.7) - do not edit
BasicModelOD$set("public", "transform_variables", function(y) {
    info <- self$info()
    set_dim <- function(x, dimx) {
        if (length(dimx) > 1L) {
            dim(x) <- dimx
        }
        x
    }
    if (is.matrix(y)) {
        n <- ncol(y)
        Map(function(i, d) set_dim(y[i, , drop = FALSE], c(d,
            n)), info$index, info$dim)
    } else if (is.array(y)) {
        n <- dim(y)[2:3]
        Map(function(i, d) set_dim(y[i, , , drop = FALSE], c(d,
            n)), info$index, info$dim)
    } else {
        Map(function(i, d) set_dim(y[i], d), info$index, info$dim)
    }
})
FullModelOD$set("public", "transform_variables", function(y) {
    info <- self$info()
    set_dim <- function(x, dimx) {
        if (length(dimx) > 1L) {
            dim(x) <- dimx
        }
        x
    }
    if (is.matrix(y)) {
        n <- ncol(y)
        Map(function(i, d) set_dim(y[i, , drop = FALSE], c(d,
            n)), info$index, info$dim)
    } else if (is.array(y)) {
        n <- dim(y)[2:3]
        Map(function(i, d) set_dim(y[i, , , drop = FALSE], c(d,
            n)), info$index, info$dim)
    } else {
        Map(function(i, d) set_dim(y[i], d), info$index, info$dim)
    }
})
